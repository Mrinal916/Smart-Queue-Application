# Copy these values into the repository-root .env file for local SMTP delivery.
# Do not commit the real .env file.
TwilioSettings__FromEmail=your-email@example.com
TwilioSettings__SmtpUsername=your-email@example.com
TwilioSettings__SmtpPassword=your-smtp-app-password
